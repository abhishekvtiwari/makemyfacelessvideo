# CLAUDE.local.md — Local Dev (gitignored)

## Local URLs

- **Frontend:** http://localhost:3000
- **Backend:** http://localhost:8000
- **Supabase Studio:** http://localhost:54323 (if running local Supabase)
- **API Docs:** http://localhost:8000/docs

## NEXT_PUBLIC_API_URL

```
NEXT_PUBLIC_API_URL=http://localhost:8000
```

## Personal Preferences

- Prefer short, direct responses — no trailing summaries
- Show full file paths in every code block
- When editing a backend file, always check the matching frontend type definition
- Run `npm run type-check` before declaring a TypeScript task done
- When a Supabase query is added, confirm the RLS policy exists for it
